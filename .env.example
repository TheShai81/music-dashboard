# Database Configuration
# Copy this file to .env and update with your local MySQL credentials
# The .env file is gitignored and won't be committed

# Database host (usually localhost for local MySQL)
DB_HOST=localhost

# Database user (use 'root' or 'spotify_user' depending on your setup)
# If using schema.sql defaults, use 'spotify_user'
# If using your own MySQL setup, use 'root' or your preferred username
DB_USER=root

# Database password
# If using schema.sql defaults, use 'Spotify123!'
# Otherwise, use your MySQL root password or custom password
DB_PASSWORD=Wall06233!

# Database name (usually spotify_db)
DB_NAME=spotify_db
